import React from 'react'
import Button from './components/Button'


const App = (props) => (
  <div>
    <Button
    onClick={() => alert('bu')}
    />
<Button
    onClick={() => alert('bu')}
    />
  </div>
)
export default App
